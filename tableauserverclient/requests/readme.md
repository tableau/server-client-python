Nothing in this folder should depend on anything in /server or /models.

This contains definitions of the Tableau Request objects. 
These are used by both the server endpoints and the client model code. 
